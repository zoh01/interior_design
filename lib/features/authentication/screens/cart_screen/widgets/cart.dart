class CartItem {
  final String imagePath;
  final String title;
  final String subTitle;
  final double price;
  final int quantity;

  CartItem(this.imagePath, this.title, this.subTitle, this.price, this.quantity);
}